# Run `powershell -ExecutionPolicy Bypass -File scripts/initialize-local-secrets.ps1`
# to create the ignored `.env` file. Never commit or reuse its generated value.
INSTALLATION_TOKEN_SECRET=replace-with-a-random-development-secret
